#include<stdio.h>
int ispalimdrome(int n);
int main()
{
    int num;
    printf("Enter a number :");
    scanf("%d",&num);
    if(ispalimdrome(num))
       printf("Palimdrome number");
       else
       printf("Not palimdrome number");
    return 0;
}
int ispalimdrome(int n)
{
    
}